<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/dialog_single_item_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="5dp"
        android:text="@string/edit_this_sticker"
        android:textColor="#fff"
        android:textSize="18sp" />

    <ImageView
        android:id="@+id/dialog_server_sticker_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="5dp"
        android:layout_below="@id/dialog_single_item_title"
        android:layout_marginTop="5dp"
        android:contentDescription="@string/stickergram"
        android:scaleType="fitCenter" />

    <ImageView
        android:id="@+id/dialog_server_sticker_error"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_centerInParent="true"
        android:src="@drawable/error" />

    <ProgressBar
        android:id="@+id/dialog_server_sticker_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />

</RelativeLayout>