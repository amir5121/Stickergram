<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/icon_sticker_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="2dp"
    android:background="@drawable/simple_sticker_background"
    android:clickable="true"
    android:foreground="?android:selectableItemBackground">

    <ImageView
        android:id="@+id/template_sticker_icon_item_stickerItem"
        android:layout_width="match_parent"
        android:layout_height="115dp"
        android:layout_gravity="center"
        android:layout_margin="2dp"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/template_sticker_icon_item_stickerName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/template_sticker_icon_item_stickerItem"
        android:layout_margin="3dp"
        android:background="@color/icon_sticker_name_background"
        android:gravity="center"
        android:padding="5dp"
        android:text="Sticker Name"
        android:textColor="#fff"
        android:textSize="17sp"
        android:textStyle="bold" />
</RelativeLayout>