<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="2dp"
    android:clickable="true">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@color/icon_sticker_list_background">

        <ImageView
            android:id="@+id/simple_sticker_item_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:background="?android:selectableItemBackground"
            android:padding="4dp"
            android:scaleType="fitCenter"
            android:src="@drawable/phone" />

        <!--todo: change the foreground color instead of overlaying another imageView on top-->

        <!--<ImageView-->
        <!--android:id="@+id/list_view_item_selectedSticker_foreground"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="100dp"-->
        <!--android:layout_gravity="center"-->
        <!--android:layout_marginBottom="10dp"-->
        <!--android:background="@drawable/selected_item_foreground"-->
        <!--android:visibility="gone" />-->

    </FrameLayout>
</android.support.v7.widget.CardView>